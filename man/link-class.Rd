% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/link_class.r
\name{link-class}
\alias{link-class}
\alias{`link`}
\title{S3 Class for Statistical Link Functions}
\description{
A generic S3 object of class \code{"link"} that encapsulates all mathematical properties
of a link function. It contains the transformation function, the inverse transformation,
and the exact analytical first and second derivatives for both directions.
}
\details{
S3 Class \code{link}

Objects of class \code{"link"} are typically created by constructor functions such as
\code{\link{logit_link}} or \code{\link{identity_link}}.

The object assumes the following mathematical notation:
\itemize{
\item \eqn{\theta}: The response parameter (e.g., probability, mean, dispersion).
\item \eqn{\eta}: The linear predictor (unconstrained scale).
}
The relationship is defined as \eqn{\eta = g(\theta)} (link function) and
\eqn{\theta = g^{-1}(\eta)} (inverse link function).
}
\section{Structure}{

An object of class \code{"link"} is a \code{list} with the following components:

\describe{
\item{\code{link_name}}{
A character string identifying the link (e.g., "logit", "identity", "probit").
}
\item{\code{link_bounds}}{
A numeric vector of length 2 \code{c(lower, upper)} defining the valid domain for \eqn{\theta}.
Values outside this range are mathematically invalid for the specific link.
}
\item{\code{link_params}}{
A list or vector of additional parameters required to define the link (e.g., \eqn{\lambda} for Power links),
or \code{NULL} if the link is parameter-free.
}
\item{\code{linkfun}}{
Function \eqn{g(\theta) \rightarrow \eta}. Transforms the parameter to the linear predictor scale.
}
\item{\code{linkinv}}{
Function \eqn{g^{-1}(\eta) \rightarrow \theta}. Transforms the linear predictor back to the parameter scale.
}
\item{\code{theta_eta}}{
Function \eqn{d\theta / d\eta}. The first derivative of the inverse link function with respect to \eqn{\eta}.
}
\item{\code{theta2_eta2}}{
Function \eqn{d^2\theta / d\eta^2}. The second derivative of the inverse link function with respect to \eqn{\eta}.
}
\item{\code{eta_theta}}{
Function \eqn{d\eta / d\theta}. The first derivative of the link function with respect to \eqn{\theta}.
}
\item{\code{eta2_theta2}}{
Function \eqn{d^2\eta / d\theta^2}. The second derivative of the link function with respect to \eqn{\theta}.
}
}
}

